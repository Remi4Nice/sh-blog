tmport Button FROM "../components/Button.js"

const Button = ((Text)) => {
  return (

    <div>
      <Button className="btns" >
        (this.props.first)
      </Button>
    </div>
  )
}

export default Button